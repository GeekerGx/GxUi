//Gx配置文件
(function () {
    var config = {
        //开发变量，设置为true则开启打印日志
        devFlag: false,
        ajax: {
            /**
             * url前缀，前后端分离后api地址填写
             */
            urlPrefix: ""
        }
    };
    Gx.config = config;
})();